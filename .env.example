# Copy this file to .env.local and add your values
# Get your Gemini key from: https://aistudio.google.com/apikey

VITE_GEMINI_API_KEY=your_gemini_api_key_here

# High-risk email alerts (Nodemailer) – used by server in ./server
# Alerts are sent to  when risk is High or Risky
SMTP_USER=your_email@example.com
SMTP_PASS=your_password_or_app_password

# Option A – Gmail: use App Password (Google Account → Security → App passwords), not normal password
# SMTP_SERVICE=gmail

# Option B – Other providers (Outlook, Yahoo, etc.): use host + port, often normal password works
# SMTP_HOST=smtp-mail.outlook.com
# SMTP_PORT=587
# SMTP_SECURE=false

# Optional: SMTP_FROM=display@email.com, SERVER_PORT=3001
